@model IEnumerable<BytPax.Models.RecordHistory>

@{
    ViewData["Title"] = "Історія рекордів";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0">🏆 Історія рекордів</h2>
    <a class="btn btn-success shadow-sm" href="@Url.Action("Create", "RecordHistory", new { area = "Admin" })">
        <i class="fas fa-plus-circle"></i> Додати рекорд
    </a>
</div>

<div class="card shadow animate__animated animate__fadeIn">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-dark text-center">
                    <tr>
                        <th>Імʼя спортсмена</th>
                        <th>Вид спорту</th>
                        <th>Рекорд</th>
                        <th>Дата</th>
                        <th>Дії</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var record in Model)
                    {
                        <tr>
                            <td>@record.AthleteName</td>
                        
                            <td>@record.RecordValue</td>
                            <td>@record.DateAchieved.ToString("dd.MM.yyyy")</td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a class="btn btn-sm btn-outline-primary"
                                       href="@Url.Action("Edit", "RecordHistory", new { id = record.Id, area = "Admin" })"
                                       title="Редагувати">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a class="btn btn-sm btn-outline-danger"
                                       href="@Url.Action("Delete", "RecordHistory", new { id = record.Id, area = "Admin" })"
                                       onclick="return confirm('Ви впевнені, що хочете видалити цей рекорд?');"
                                       title="Видалити">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
