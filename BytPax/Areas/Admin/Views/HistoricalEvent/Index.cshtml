@model IEnumerable<BytPax.Models.HistoricalEvent>

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0">📜 Історичні події</h2>
    <a class="btn btn-success" href="@Url.Action("Create", "HistoricalEvent", new { area = "Admin" })">➕ Додати подію</a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-striped table-bordered table-hover mb-0">
            <thead class="table-dark">
            <tr>
                <th>Назва події</th>
                <th>Опис</th>
                <th>Дата</th>
                <th>Зображення</th>
                <th class="text-center">Дії</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var ev in Model)
            {
                <tr>
                    <td>@ev.Title</td>
                    <td>@ev.Description</td>
                    <td>@ev.EventDate.ToShortDateString()</td>
                    <td>@ev.ImagePath</td>
                    <td>
                        <div class="d-flex justify-content-center gap-2">
                            <a class="btn btn-primary btn-sm"
                               href="@Url.Action("Edit", "HistoricalEvent", new { id = ev.Id, area = "Admin" })">
                                ✏️ Редагувати
                            </a>

                            <a class="btn btn-danger btn-sm"
                               href="@Url.Action("Delete", "HistoricalEvent", new { id = ev.Id, area = "Admin" })"
                               onclick="return confirm('Ви впевнені, що хочете видалити цю подію?');">
                                🗑️ Видалити
                            </a>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
